{
  "name": "croatia-trip-hq",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "vercel dev",
    "start": "node Server/server.js",
    "test": "node --test",
    "deploy:staging": "npm test && vercel deploy --target=preview --yes",
    "deploy:production": "node scripts/blocked-production-deploy.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.3",
    "@upstash/redis": "latest"
  }
}
