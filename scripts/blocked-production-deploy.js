console.error('Direct production deploys are disabled for Croatia Trip HQ.');
console.error('Deploy to staging/preview first with: npm run deploy:staging');
console.error('Only promote to production after Tanner explicitly approves a verified staging URL.');
process.exit(1);
